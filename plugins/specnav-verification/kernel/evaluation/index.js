'use strict';

const {
  createOracleRegistry
} = require('./oracle-registry');
const {
  createReadingEvaluator
} = require('./reading-evaluator');

module.exports = {
  createOracleRegistry,
  createReadingEvaluator
};
